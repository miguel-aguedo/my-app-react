import React from 'react';

const Saludo = (msj) => {
    return(
        <div>
            <h1>Hola mundo desde React...</h1>
            <h2>Mensaje: {msj.variable}</h2>
        </div>

    );

}
export default Saludo;